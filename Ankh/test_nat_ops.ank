
set_max_nat_bits(256);

nat max(nat x, nat y) {
	if (x > y) {
		x;
	}
	else {
		y;
	}
}

nat add(nat x, nat y) {
	x + y;
}

nat add10(nat x, nat y) {
	y + 5 + x + 5n;
}

nat sub(nat x, nat y) {
	x - y;
}

nat no_opt assign(nat x) {
	nat y;
	y = x;
	y;
}

int add_in_place(nat x, nat y) {
	x = x + y;
	0;
}